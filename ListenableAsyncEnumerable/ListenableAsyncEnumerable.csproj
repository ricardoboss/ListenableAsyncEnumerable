<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <TargetFramework>net7.0</TargetFramework>
        <ImplicitUsings>enable</ImplicitUsings>
        <Nullable>enable</Nullable>
        <LangVersion>default</LangVersion>
        <RootNamespace>System.Collections.Generic</RootNamespace>
        <GeneratePackageOnBuild>true</GeneratePackageOnBuild>
        <Description>`ListenableAsyncEnumerable` is a C# implementation of [Dart](https://dart.dev)s `Stream` infrastructure.

It adds functionality to `IAsyncEnumerable` instances to be able to listen to the data emitted by them.</Description>
        <Copyright>2023 Ricardo Boss</Copyright>
        <PackageProjectUrl>https://github.com/ricardoboss/ListenableAsyncEnumerable</PackageProjectUrl>
        <PackageLicenseExpression>MIT</PackageLicenseExpression>
        <RepositoryUrl>https://github.com/ricardoboss/ListenableAsyncEnumerable</RepositoryUrl>
        <RepositoryType>git</RepositoryType>
        <PackageTags>IAsyncEnumerable, Async, Enumerable, Listen</PackageTags>
        <PackageReleaseNotes>Initial release</PackageReleaseNotes>
    </PropertyGroup>
</Project>
